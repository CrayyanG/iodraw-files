```mermaid
@startuml
left to right direction

rectangle "本地HTML前端\nV1.0检测软件界面" as FE
rectangle "推理服务 FastAPI+Uvicorn\n0.0.0.0:8000 + CORS" as API
rectangle "输入处理\n解码/校验/max_side缩放(scale)" as PRE
rectangle "滑动切片 Tiling\ntile_size + overlap" as TILE
rectangle "模型推理 RT-DETR-SOEP-RFPN\nUltralytics RTDETR fork\nweights: RT-DETR-SOEP-RFPN.bt" as MODEL
rectangle "坐标映射\npatch→原图坐标" as MAP
rectangle "合并后处理 Merge\nNMS/Soft-NMS/WBF\niou + conf" as MERGE
rectangle "输出 JSON\nimage_w/h + params + detections + time_cost_ms\n可选 overlay_image_base64" as OUT
database "日志/记录\n参数+耗时+摘要" as LOG

FE --> API : HTTP POST /infer\nmultipart(image)+params
API --> PRE
PRE --> TILE
TILE --> MODEL
MODEL --> MAP
MAP --> MERGE
MERGE --> OUT
OUT --> FE : JSON响应
API --> LOG

@enduml
```