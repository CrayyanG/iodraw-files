```mermaid
graph TD
    A[fa:fa-user Tom] -->|Get money| B(Go shopping)
    B --> C{Select}
    C -->|One| D[Laptop]
    C -->|Two| E[iPhone]
    C -->|Three| F[fa:fa-car Car]

  A[本地HTML前端<br/>V1.0检测软件界面] -->|HTTP POST /infer<br/>multipart(image)+params| B[推理服务 FastAPI+Uvicorn<br/>0.0.0.0:8000 + CORS]

  B --> C[输入处理<br/>解码/校验/尺寸限制max_side/缩放scale]
  C --> D[滑动切片 Tiling<br/>tile_size + overlap<br/>生成patch+offset]
  D --> E[模型推理 RT-DETR-SOEP-RFPN<br/>Ultralytics RTDETR fork<br/>weights: RT-DETR-SOEP-RFPN.bt]
  E --> F[坐标映射<br/>patch坐标 → 原图坐标]
  F --> G[合并后处理 Merge<br/>NMS / Soft-NMS / WBF<br/>iou + conf]
  G --> H[输出 JSON<br/>image_w/h + params + detections + time_cost_ms<br/>可选 overlay_image_base64]

  H -->|返回结果| A

  %% 可选：数据落盘/日志
  B --> I[(日志/记录<br/>请求参数+耗时+结果摘要)]
```