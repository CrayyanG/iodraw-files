```mermaid
graph LR
    FE[fa:fa-desktop 本地HTML前端<br/>V1.0检测软件界面] -->|HTTP POST /infer<br/>multipart(image)+params| API[fa:fa-server 推理服务<br/>FastAPI + Uvicorn<br/>0.0.0.0:8000 + CORS]

    API --> PRE[fa:fa-image 输入处理<br/>解码/校验/尺寸限制max_side<br/>缩放并返回scale]
    PRE --> TILE[fa:fa-th 滑动切片 Tiling<br/>tile_size + overlap<br/>生成patch+offset]
    TILE --> INFER[fa:fa-microchip Patch推理<br/>RT-DETR-SOEP-RFPN.bt<br/>Ultralytics RTDETR fork]
    INFER --> MAP[fa:fa-arrows 坐标映射<br/>patch坐标 → 原图坐标]
    MAP --> MERGE[fa:fa-object-group 合并后处理 Merge<br/>NMS / Soft-NMS / WBF<br/>iou + conf]
    MERGE --> OUT[fa:fa-code 输出JSON<br/>image_w/h + params<br/>detections[xyxy,score,cls,label]<br/>time_cost_ms<br/>可选overlay_base64]

    OUT -->|JSON响应| FE
```